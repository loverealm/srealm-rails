%div.actions.follow-container
  %p.big
  %p.pull-left
  %h2 Feedbacks
  - @feedbacks.each do |feedback| next unless feedback.user
    .following-container.clearfix
      = link_to show_avatar(feedback.user, "thumb"),home_path
      %i
        = feedback.subject
      .text-holder
        %h6 #{feedback.user.full_name}
        %br
        %span
          = link_to 'See feedback', [:admin, feedback]
  = paginate @feedbacks

