#modal_invite_friends.modal.fade{:role => "dialog", :tabindex => "-1", 'data-backdrop'=>''}
  .modal-dialog{:role => "document"}
    .modal-content
      .modal-header
        %button.close{"aria-label" => "Close", "data-dismiss" => "modal", :type => "button"}
          %span{"aria-hidden" => "true"} ×
        %h4.modal-title Invite Friends
      .modal-body
        = render file: '/dashboard/welcome/invite_people', locals:{ for_modal: true }
= content_for :script do
  :javascript
    jQuery(function(){
      setTimeout(function(){
        $('#modal_invite_friends').modal().on('hidden.bs.modal', function(e){ $.get('#{mark_shown_invite_friends_url}') })
      }, 500);
    });